import styled from 'styled-components'

import { primaryColor } from '../../utilsStyles/variables'

export const HeaderTag = styled.header`
  background: ${primaryColor};
  height: 120px;
  width: 100%;
`